<?php

if (defined ('ROOT_DIR')) {

/* ============================================================
 * 🌍 Режим окружения / Environment mode
 * Возможные значения: 'production', 'development'
 * На боевом сайте ставьте ТОЛЬКО 'production'
 * Possible values: 'production', 'development'
 * Always set to 'production' on a live site!
 * ============================================================ */
define('ENVIRONMENT', 'production');


/* ============================================================
 * 🔑 Основной ключ группы / Main group key
 * Хранится в админ панели
 * Stored in the admin panel
 * ============================================================ */
define('API_KEY', "%API_KEY%");


/* ============================================================
 * 🔗 Центральное API / Central API
 * Это URL центрального API
 * This is the central API URL
 * ============================================================ */
define('API_URL', "https://api.mmoweb.biz/");


/* ============================================================
 * 🗄️ Подключение к базе данных / Database connection
 * Настраивается подключение к базе хостинга
 * Configure access to your hosting database
 * ============================================================ */
define('DB_HOST',       '%DB_HOST%');
define('DB_NAME',       '%DB_NAME%');
define('DB_USER',       '%DB_USER%');
define('DB_PASSWORD',   '%DB_PASSWORD%');


/* ============================================================
 * ⚙️ Общие настройки / General settings
 * ============================================================ */
define('DEBUG', FALSE);             // Включить режим отладки / Enable debugging mode
define('DEBUG_IP', '127.0.0.1');    // Разрешённый IP для отладки / Allowed IP for debugging
define('DETECT_LANG', TRUE);        // Автоопределение языка / Auto-detect user language
define('UTF8_ENABLED', TRUE);       // Использовать UTF-8 / Use UTF-8 encoding
define("VIEWPATH", '/template');    // Путь к шаблонам / Path to templates
define("CACHEPATH", '/cache');      // Путь к кэшу / Path to cache

define('TEMPLATE', 'default'); // По умолчанию: "default" / Default: "default"
define('TEMPLATE_DIR', VIEWPATH . '/site/' . TEMPLATE); // Полный путь к файлам шаблона / Full path to template files


/* ============================================================
 * 📡 Лимиты API / API request limits
 * ============================================================ */
define('CONNECTION_MAX_COUNT', 1);  // Максимальное количество соединений / Max simultaneous connections
define('CONNECTION_TIME', 2);       // Таймаут соединения в секундах / Connection timeout (sec)


/* ============================================================
 * 🛠️ Админ-панель / Admin panel
 * По умолчанию: yoursite/admin
 * By default: yoursite/admin
 * ============================================================ */
define('ADMIN_PANEL', true);             // Включить/выключить админ-панель / Enable or disable admin panel
define('ADMIN_LOGIN', '%ADMIN_LOGIN%');  // Логин администратора / Administrator login
define('ADMIN_PASSWORD', '%ADMIN_PASSWORD%'); // Пароль администратора / Administrator password
define('ADMIN_IP', '*');                 // Разрешённые IP ( * - все ) / Allowed IPs (* = all)
define('ADMIN_URL', 'admin');            // URL к админке / URL path to admin panel (/admin)


/* ============================================================
 * ✍️ Редактор / Editor
 * ============================================================ */
define('TEMPLATE_WYSIWYG', true); // Использовать WYSIWYG редактор или textarea / Use WYSIWYG HTML Editor (true) or textarea (false)


/* ============================================================
 * ⚡ Продвинутые настройки / Advanced settings
 * ============================================================ */
define('CLOUD_FLARE', true);           // Переопределение IP через Cloudflare / Correct IP addresses with Cloudflare
define('HEADER_IP', 'REMOTE_ADDR');    // Источник IP по умолчанию / Default IP source


/* ============================================================
 * 🗂️ Кэширование / Caching (в секундах / in seconds)
 * Управление кэшем разных разделов сайта
 * Control caching of different site sections
 * ============================================================ */
define('CACHE_NEWS', 600);
define('CACHE_IBLOCK', 600);
define('CACHE_FORUM', 600);
define('CACHE_ONLINE', 600);
define('CACHE_RATING', 600);
define('CACHE_HISTORY_ONLINE', 86400);
define('CACHE_STREAM', 600);
define('CACHE_CLEAR_SESSION', 1800);
define('DEL_CACHE_THROTTLER', 86400);
define('DEL_CACHE_DEBUG', 604800);


/* ============================================================
 * 🎮 Отображение предметов / Item visualization
 * Визуальная подгрузка предметов из базы кабинета
 * Visual loading of items from the cabinet database
 * Пример / Example:
 * <span class='webdb-item' webdb-id="3936" s-name="true" s-icon="true"></span>
 * ============================================================ */
define('WEB_ITEM_DB', true);
define('ACAO_DB_GETAWAY', '*');


/* ============================================================
 * 💳 Платёжный шлюз / Payment gateway
 * ⚠ Использовать только в крайней необходимости
 * Use only in extreme cases!
 * Функция нестабильна, возможны проблемы доставки платежей
 * Unstable, may cause issues with payment delivery
 * ============================================================ */
define('PAYMENT_GATEWAY', FALSE);
define('PAYMENT_KEY', 'CHANGE_THIS_KEY');


/* ============================================================
 * 🔒 HTTPS редиректы / HTTPS forwarding
 * Для хостеров: если у вас проблемы с заголовками (HTTP_X_FORWARDED_PROTO и т.д.)
 * If you have issues with HTTPS headers, disable redirect loop
 * ============================================================ */
define('HTTP_FORWARDING', false); // Производить ли редирект если включено https / Redirect if https enabled but request is http


/* ============================================================
 * 📥 Поддержка / Support plugin
 * Настройки плагина поддержки и загрузки файлов
 * Support plugin settings and file upload
 * ============================================================ */
define('LIMIT_FILES', 5); // Лимит файлов / Max number of files
define('MAX_SIZE', 2);    // Максимальный размер файла (МБ) / Max file size (MB)

/**
 * ============================================================
 * 💳 Интеграции платежных систем / Payment systems integrations
 * Здесь можно добавить свои платёжные методы
 * You can add your own payment methods
 * ============================================================
 */
define('PAYMENT_INTEGRATIONS', [
    //'freekassa' => \Modules\Globals\Donations\Integrations\Handlers\FreeKassa::class,
]);

}
